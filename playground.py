from BitVector import *
poo = 'testing'
print(len(poo))
if len(poo) > 0:
    for j in range(0,16):
        if (len(poo) != 64):
            for i in range(64-)
'''
if bitvec.length() > 0:
    for j in range(0, 16):
        if bitvec.length() != 64:
            for i in range(64 - (bitvec.length()) % 64):
                bitvec.pad_from_right(1)'''